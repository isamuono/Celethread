class SchedulesController < ApplicationController
  def index
    #@events = Event.where(community_id: params[:community_id])
    # render :json => @event
    #respond_to do |format| # 場合わけ
    #  format.html {}
    #  format.json {
    #    render json:
    #    @event.to_json(
    #      only: [:title, :startdate, :enddate, :allday, :place] #:color]
    #    )
    #  }
    #end
  end
end